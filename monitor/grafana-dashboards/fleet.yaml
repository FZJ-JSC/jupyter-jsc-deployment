defaultNamespace: jupyter
targetCustomizations:
  - name: staging
    clusterSelector:
      matchLabels:
        stage: staging
  - name: production
    clusterSelector:
      matchLabels:
        stage: production
