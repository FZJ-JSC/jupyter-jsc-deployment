defaultNamespace: jupyterjsc
targetCustomizations:
  - name: staging
    clusterSelector:
      matchLabels:
        stage: staging
    helm:
      values:
        stage: "staging"
        enabled: false
  - name: production
    clusterSelector:
      matchLabels:
        stage: production
    helm:
      values:
        stage: "production"
        enabled: false