# Does not work for v0.11.x, but keep around for upgrade
hub:
  services:
    metrics:
      name: metrics-prometheus-monitor
  loadRoles:
    metrics-role:
      description: Access to metrics information for Prometheus ServiceMonitor
      scopes: [read:metrics]
      services: [metrics-prometheus-monitor]
