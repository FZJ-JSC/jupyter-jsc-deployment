apiVersion: v1
kind: Service
metadata:
  name: hub-static-files
spec:
  ports:
  - name: hub-static
    port: 8070
    protocol: TCP
    targetPort: 8070
  selector:
    app: jupyterhub
    component: hub
  type: ClusterIP
