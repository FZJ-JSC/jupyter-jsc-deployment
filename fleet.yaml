defaultNamespace: jupyter
helm:
  releaseName: jupyterjsc
  repo: https://hub.jupyter.org/helm-chart/
  chart: jupyterhub
  version: 3.0.3
  values:
    prePuller:
      hook:
        enabled: false
    ingress:
      enabled: true
      hosts:
        - zam12065.zam.kfa-juelich.de
