---
defaultNamespace: portalgauss

labels:
  app: portalgauss
  component: z2jh

dependsOn:
  - selector:
      matchLabels:
        app: portalgauss
        component: secrets
  - selector:
      matchLabels:
        app: postgres-operator
        component: namespaces

kustomize:
  dir: ./kustomize
helm:
  releaseName: portalgauss
  # repo: https://jupyterhub.github.io/helm-chart/
  # chart: jupyterhub
  # version: 3.1.0
  chart:
    https://github.com/jupyterhub/helm-chart/raw/gh-pages/jupyterhub-4.3.2.tgz
  valuesFiles:
    - ../../base-values/extraConfig.yaml
    - ../../base-values/image.yaml
  valuesFrom:
    - configMapKeyRef:
        name: values
        namespace: portalgauss
        key: values.yaml
    - secretKeyRef:
        name: static-hub-secret
        namespace: portalgauss
        key: values.yaml

targetCustomizations:
  - name: production
    clusterSelector:
      matchLabels:
        stage: production
    helm:
      valuesFiles:
        - ../../base-values/resources.yaml
