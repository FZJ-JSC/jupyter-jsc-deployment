apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: {{ template "k8smgr.name" . }}-certs
  labels:
    app: {{ template "k8smgr.name" . }}
    chart: {{ template "k8smgr.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
data:
{{ ( include "k8smgr.gen-certs" . ) | indent 2 }}
