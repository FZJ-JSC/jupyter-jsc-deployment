defaultnamespace: juniq
dependsOn:
  - selector:
      matchLabels:
        create-namespaces: "true"

doNotDeploy: true
targetCustomizations:
  - name: staging
    clusterSelector:
      matchLabels:
        stage: staging
    doNotDeploy: false