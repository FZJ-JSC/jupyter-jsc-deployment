defaultnamespace: juniq
dependsOn:
  - selector:
      matchLabels:
        create-namespaces: "true"

doNotDeploy: true
targetCustomizations:
  - name: production
    clusterSelector:
      matchLabels:
        stage: production
    doNotDeploy: false