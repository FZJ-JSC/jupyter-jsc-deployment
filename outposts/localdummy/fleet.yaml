defaultNamespace: localdummy
kustomize:
  dir: ./kustomize
helm:
  releaseName: localdummy
  repo: https://kaas.pages.jsc.fz-juelich.de/helm-charts
  chart: jupyterhub-outpost
  version: 1.0.3
  values:
    cryptSecret: outpost-cryptkey
    outpostUser: outpost-users
  valuesFiles:
    - values/config.yaml
