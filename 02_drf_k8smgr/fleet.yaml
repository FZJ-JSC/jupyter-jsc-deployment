defaultNamespace: userlabs
helm:
  values:
    image:
      tag: "1.0.4-2"
    nameOverride: "drf-k8smgr"
    nodeSelector:
      worker: "true"
targetCustomizations:
  - name: staging
    clusterSelector:
      matchLabels:
        stage: staging
    helm:
      values:
        gitconfig:
          branch: "k8smgr-staging"
