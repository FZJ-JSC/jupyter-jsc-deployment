defaultNamespace: jupyterjsc
helm:
  values:
    image:
      tag: "1.1.3"
    nodeSelector:
      worker: "true"
    podAnnotations:
      proxy.istio.io/config: '{ "holdApplicationUntilProxyStarts": true }'
