defaultnamespace: euroccgcs

labels:
  app: euroccgcs
  component: z2jh

dependsOn:
  - selector:
      matchLabels:
        app: euroccgcs
        component: secrets
  - selector:
      matchLabels:
        app: postgres-operator
        component: namespaces
  - selector:
      matchLabels:
        app: postgres-operator
        component: databases

kustomize:
  dir: ./kustomize
helm:
  releaseName: euroccgcs
  # repo: https://jupyterhub.github.io/helm-chart/
  # chart: jupyterhub
  # version: 3.1.0
  chart: https://github.com/jupyterhub/helm-chart/raw/gh-pages/jupyterhub-3.3.8.tgz
  values:
    ingress:
      # Set JupyterHub in maintenance:
      #   Set enabled here to false, update ingress in maintenance/fleet.yaml
      enabled: true
  valuesFiles:
    - ../../base-values/extraConfig.yaml
    - ../../base-values/image.yaml
  valuesFrom:
    - configMapKeyRef:
        name: values
        namespace: euroccgcs
        key: values.yaml
    - secretKeyRef:
        name: static-hub-secret
        namespace: euroccgcs
        key: values.yaml

targetCustomizations:
  - name: production
    clusterSelector:
      matchLabels:
        stage: production
    helm:
      valuesFiles:
        - ../../base-values/image_fixed.yaml
    kustomize:
      dir: ./kustomize
