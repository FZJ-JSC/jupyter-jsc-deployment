defaultNamespace: jupyterjsc
helm:
  values:
    image:
      tag: "1.0.3-9"
    nameOverride: "drf-tunnel"
    secrets:
      keypairs: "drf-tunnel-keypairs"
      passwds: "drf-tunnel-passwds"
      databaseGeneral: "postgresql-general"
      databaseUsers: "postgresql-users"
    imagePullSecrets:
    - name: "gitlab-registry"
