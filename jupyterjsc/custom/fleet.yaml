defaultNamespace: jupyterjsc

labels:
  app: jupyterjsc
  component: custom

dependsOn:
  - selector:
      matchLabels:
        app: postgres-operator
        component: namespaces

helm:
  releaseName: jupyterjsc-custom
  repo: https://kaas.pages.jsc.fz-juelich.de/helm-charts
  chart: jupyter-hub-customizations
  version: 0.15.0
  values:
    fullnameOverride: jupyterjsc-custom
    global:
      name: jupyterjsc
      namespace: jupyterjsc
    customConfig:
      announcement:
        show: false
        title: "Jupyter-JSC is now running on JSC-Cloud"
        body: "We have moved Jupyter-JSC to the JSC-Cloud. All data from HDF-Cloud was moved to JSC-Cloud."
    ingress:
      enabled: true
    jupyter-metrics:
      enabled: true
      secretSuffixes:
        - metrics
        - metrics-codebase
    jupyter-user-kpi:
      enabled: true
      secretSuffixes:
        - user-kpi-codebase
    logging:
      syslog:
        enabled: true
        level: 10
targetCustomizations:
  - name: jupyter-stag
    clusterSelector:
      matchLabels:
        stage: staging
    helm:
      values:
        global:
          # hostname: zam12246.zam.kfa-juelich.de
          hostname: jupyter-staging.jsc.fz-juelich.de
          baseUrl: /
          loggerName: JupyterJSCStaging
        nfs:
          path: /nfs/hubs/rke2/staging/jupyterjsc
        redirects:
          - name: "staging"
            oldHost: "jupyter-jsc-staging.fz-juelich.de"
            newHost: "jupyter-staging.jsc.fz-juelich.de"
          - name: "staging-nbviewer"
            oldHost: "docs.jupyter-jsc-staging.fz-juelich.de"
            newHost: "docs.jupyter-staging.jsc.fz-juelich.de"
  - name: jupyter-prod
    clusterSelector:
      matchLabels:
        stage: production
    helm:
      values:
        global:
          hostname: jupyter.jsc.fz-juelich.de
          loggerName: JupyterJSC
        nfs:
          path: /nfs/hubs/rke2/production/jupyterjsc
        redirects:
          - name: "production"
            oldHost: "jupyter-jsc.fz-juelich.de"
            newHost: "jupyter.jsc.fz-juelich.de"
          - name: "production-nbviewer"
            oldHost: "docs.jupyter-jsc.fz-juelich.de"
            newHost: "docs.jupyter.jsc.fz-juelich.de"