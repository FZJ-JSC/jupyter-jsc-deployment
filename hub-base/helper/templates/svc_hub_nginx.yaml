apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceStatic.name }} 
spec:
  ports:
  - name: hub-static
    port: {{ .Values.serviceStatic.port }} 
    protocol: TCP
    targetPort: 8070
  selector:
    app: {{ .Values.nginx.service.selector.app }}
    component: hub
  type: ClusterIP
