defaultNamespace: database
helm:
  releaseName: ceticadminer
  repo: https://cetic.github.io/helm-charts
  chart: adminer
  values:
    service:
      type: ClusterIP
    nodeSelector:
      worker: "true"
dependsOn:
  - name: basics-postgresql
