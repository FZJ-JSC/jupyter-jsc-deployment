defaultNamespace: database
helm:
  releaseName: ceticadminer
  repo: https://cetic.github.io/helm-charts
  chart: adminer
  version: 0.2.1
  values:
    service:
      type: ClusterIP
    nodeSelector:
      worker: "true"
dependsOn:
  - name: basics-postgresql-ha
