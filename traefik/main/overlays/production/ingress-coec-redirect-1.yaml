---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-cluster-issuer
    nginx.ingress.kubernetes.io/permanent-redirect: https://jupyterjsc.pages.jsc.fz-juelich.de/docs/jupyterjsc/
  name: coec-redirect-1
  namespace: traefik
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - docs.jupyter-coec.jsc.fz-juelich.de
    secretName: coec-redirect-1
  rules:
  - host: docs.jupyter-coec.jsc.fz-juelich.de
