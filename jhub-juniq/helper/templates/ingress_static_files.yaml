{{- if .Values.ingresshubstatic.enabled -}}
{{- $fullName := .Values.serviceStatic.name -}}
{{- $svcPort := .Values.serviceStatic.port -}}
{{- if and .Values.ingresshubstatic.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ingresshubstatic.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ingresshubstatic.annotations "kubernetes.io/ingress.class" .Values.ingresshubstatic.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "helper.labels" . | nindent 4 }}
  {{- with .Values.ingresshubstatic.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ingresshubstatic.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ingresshubstatic.className }}
  {{- end }}
  {{- if .Values.ingresshubstatic.tls }}
  tls:
    {{- range .Values.ingresshubstatic.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range $hosti := .Values.ingresshubstatic.hosts }}
    - host: {{ $hosti.host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $hosti.svcName }}
                port:
                  number: {{ $hosti.svcPort }}
              {{- else }}
              serviceName: {{ $hosti.svcName }}
              servicePort: {{ $hosti.svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
