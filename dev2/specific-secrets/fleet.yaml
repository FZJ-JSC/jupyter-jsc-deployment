defaultnamespace: dev2

labels:
  app: dev2
  component: secrets

dependsOn:
  - selector:
      matchLabels:
        app: sealed-secrets

targetCustomizations:
- name: jupyter-stag
  clusterSelector:
    matchLabels:
      stage: staging
  kustomize:
    dir: overlays/staging
- name: jupyter-prod
  clusterSelector:
    matchLabels:
      stage: production
  doNotDeploy: true